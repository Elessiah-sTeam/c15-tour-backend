spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/c15tourdb
    username: devuser
    password: devpassword
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true